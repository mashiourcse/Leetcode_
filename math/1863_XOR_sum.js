/**
 * @param {number[]} nums
 * @return {number}
 */

const returnXOR = (a,b)=>{

    return a^b;
  } 
  var subsetXORSum = function(nums) {
      
  };
  
  var x = [5,1,6];
  
  console.log(5^1);
  console.log(4^6);
  console.log(5^1^6);
  
  // generate subsets from array
  // generate xor of each subsets
  // sum the xors